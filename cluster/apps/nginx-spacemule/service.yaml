apiVersion: v1
kind: Service
metadata:
  name: nginx-spacemule-service
spec:
  type: ClusterIP
  selector:
    app: nginx-spacemule
  ports:
    - name: nginx-spacemule-service
      protocol: TCP
      port: 8081
      targetPort: nginx-http