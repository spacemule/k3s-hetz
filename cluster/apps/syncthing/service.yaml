apiVersion: v1
kind: Service
metadata:
  name: syncthing-service
spec:
  type: ExternalName
  externalName: sync.spacemule.net
  selector:
    app: syncthing
  ports:
    - name: syncthing-gui-service
      protocol: TCP
      port: 8080
      targetPort: syncthing-gui