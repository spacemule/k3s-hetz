apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-zahava-pvc
  namespace: nginx-static
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi